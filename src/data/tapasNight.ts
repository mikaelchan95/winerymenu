import { MenuItem } from '../types';

/**
 * Tapas Night items are now loaded from Supabase
 * This file provides the export structure for compatibility
 */
export const tapasNightItems: MenuItem[] = [];